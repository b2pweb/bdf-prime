build:
  nodes:
    tests:
      environment:
        timezone: FR
        php:
          version: 7.4

      services:
        mysql: 5.7

      tests:
        override:
          - command: composer run tests-with-coverage
            coverage:
              file: coverage.xml
              format: clover