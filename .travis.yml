language: php

services: mysql

php:
  - 7.1
  - 7.2
  - 7.3
  - 7.4
  - 8.0

jobs:
  include:
    - stage: analysis
      php: 7.2
      script:
        - composer psalm

install:
  - travis_retry composer install

script:
  - composer tests
